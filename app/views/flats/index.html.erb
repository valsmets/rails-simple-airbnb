<h1>Airbnb</h1>
<ul>
  <% @flats.each do |flat| %>
    <li><%= link_to flat.name, flat_path(flat) %> |<%= link_to 'edit', edit_flat_path(flat) %> | <%= link_to "Delete #{flat.name}",
            flat_path(flat),
            method: :delete,
            data: { confirm: "Are you sure?" } %> </li>
  <% end %>
</ul>

<%= link_to "Add a new flat", new_flat_path %>
